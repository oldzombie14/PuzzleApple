# PuzzleApple 协作约定

## 沟通与执行
- 日常任务中，遇到影响目标、范围、设计或验收的不明确之处，及时向用户询问，尽量在正式执行前集中澄清。
- 目标清楚、没有待确认问题后，正式开始时标注 `【开始任务】`。
- 不要先做一部分依赖未确认信息的工作，再中途提问。执行中出现此前无法预见的关键问题时，仍应及时说明并询问，不要擅自决定。
- 用户明确说 `【直接执行】` 时，按其大致意图自主推进，采用合理默认值，不为常规澄清中断任务；完成后说明重要假设与结果。此约定不替代必须遵守的权限与安全要求。

## 文档与上下文
- 项目文档统一放在 `Assets/Docs/`（用户创建的 docs 文件夹），不要另建重复入口。
- 从 `Assets/Docs/README.md` 开始阅读，按任务需要查阅设计、要求与进度。
- 按需维护有助于理解宏观方向、已确认需求、重要决策和当前进度的文档；保持简洁，避免逐条记录聊天或堆积重复文件。
- 区分用户已确认的要求、待确认事项和实现假设，不能将推测写成既定设计。

## 文件归类
- 核心目标是保持 `Assets/` 根目录简洁有序；脚本只是分类示例，这一原则适用于所有文件和资源。
- 可按实际需要建立、细分或整理任何资源类别的目录，不必局限于当前分类。常规整理可自主处理；涉及设计含义或较大结构取舍时再询问用户。
- 沿用现有目录：脚本放在 `Assets/Scripts/`，需要细分时在其下按功能建子目录。
- 场景放在 `Assets/Scenes/`，预制体放在 `Assets/Prefabs/`，3D 美术资源放在 `Assets/ArtAssets-3D/`。
- 新的资源类别可按需建立独立目录，例如 `Assets/Shaders/`；分类存在实质歧义时询问用户。
- 不要为小任务随意在根目录或 Assets 根目录散放文件；整理已有 Unity 资源时保留对应 `.meta` 和 GUID，维护资源引用，避免无意义的目录改动。

## 无用内容清理
- 随功能和设计方向变化，可在用户要求时或自主判断有必要时，清理已失效、被替代或确认不再使用的内容，重点是代码，也包括相关资源和过时文档。
- 清理必须保持当前功能和有效设计不变，不借清理擅自移除仍有效的功能或改变行为。
- 删除前核实用途和引用，包含场景、预制体、序列化字段、UnityEvent、反射及运行时动态加载等；不能仅凭代码搜索没有调用就判定无用。
- 用途不明、是否保留尚未确认的内容，先保留并澄清；用户说 `【直接执行】` 时保守跳过不确定项，在结果中说明。
- 优先采用范围明确、可回退的清理，保留无关的用户改动；按影响范围检查编译、资源引用和相关功能，必要时运行测试或进行实际验证。
- 完成后简要说明清理内容、验证结果和仍未验证的部分；重要变化同步更新项目文档。

## 更改报告
- 用户说 `【更改报告】` 时，提供一份简短 brief，解释已完成的更改及大致思路，不因此开始新的修改。
- 默认汇报最近一次任务的更改；用户指定范围时按其范围汇报。连续多轮围绕同一任务时，合并说明该任务的相关更改。
- 初始结构：改了什么（主要结果和涉及范围）、大致思路（为什么这样做、如何实现）、验证情况（已检查内容和未验证部分）；有必要时补充当前功能影响或待确认事项。
- 使用简洁易懂的语言，优先说明行为和设计变化，不堆砌文件名、代码细节或工具调用过程；必要时链接关键文件。
- 报告依据实际完成的工作，区分已完成、计划和假设；具体结构可在实战中按用户反馈优化。
